<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.chatting_user">
	<select id="showAll" parameterType="int" resultType="ChattingVO">
		select * from chatting_user where chat_user_exist= 'Y' and group_no=#{groupNo}
	</select>
</mapper>